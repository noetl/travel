apiVersion: noetl.io/v2
kind: Playbook
metadata:
  name: adiona_item_attribute_translation_delete
  path: adiona/v1/item_attribute_translation_delete
workload:
  request: {}
workflow:
  - step: start
    tool:
      kind: postgres
      auth: adiona_actor
      params:
        - "{{ request | tojson | b64encode }}"
      command: |
        WITH p AS MATERIALIZED (SELECT convert_from(decode($1::text,'base64'),'UTF8')::jsonb AS j), changed AS (DELETE FROM adiona.item_attribute_content USING p WHERE item_attribute_id=(j->>'item_attribute_id')::bigint AND lang_code=(j->>'lang_code')::text RETURNING *) SELECT to_jsonb(result_row) AS result FROM (SELECT * FROM changed) result_row;
