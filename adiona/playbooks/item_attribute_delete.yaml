apiVersion: noetl.io/v2
kind: Playbook
metadata:
  name: adiona_item_attribute_delete
  path: adiona/v1/item_attribute_delete
workload:
  request: {}
workflow:
  - step: start
    tool:
      kind: postgres
      auth: adiona_actor
      params:
        - "{{ request | tojson | b64encode }}"
      command: |
        WITH p AS MATERIALIZED (SELECT convert_from(decode($1::text,'base64'),'UTF8')::jsonb AS j), changed AS (DELETE FROM adiona.item_attributes USING p WHERE item_id=(j->>'item_id')::bigint AND attribute_id=(j->>'attribute_id')::bigint RETURNING *) SELECT to_jsonb(result_row) AS result FROM (SELECT * FROM changed) result_row;
